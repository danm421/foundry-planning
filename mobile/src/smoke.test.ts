import { expect, test } from "vitest";

// Placeholder so `npm test` has something to run against the freshly
// scaffolded mobile app. Task 6 replaces this with real tests.
test("scaffold smoke test", () => {
  expect(1).toBe(1);
});
