// Sub-tab loading boundary for the tax-analysis section. Mirrors the
// cashflow/loading.tsx pattern — paints instantly in the content area while
// the server page resolves client access.
export { default } from "@/components/skeleton/section-loading";
