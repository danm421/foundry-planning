// STUB — replaced in Task 12 with the AI prose panel (generate/regenerate,
// staleness banner, snapshot/goals/opportunities sections).
"use client";

export function GeneratePanel(_: {
  clientId: string;
  stale: boolean;
  initial: {
    snapshot: string;
    goals: string;
    opportunities: string;
    generatedAt: string;
    model: string;
  } | null;
}) {
  return null;
}
