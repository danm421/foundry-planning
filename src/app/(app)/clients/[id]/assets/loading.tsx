// Sub-tab loading boundary for the assets section. Sits below assets/layout.tsx
// (the subtab strip / sidebar), so navigating between assets sub-tabs keeps the
// subtabs in place and paints this skeleton in the content area instantly.
export { default } from "@/components/skeleton/section-loading";
