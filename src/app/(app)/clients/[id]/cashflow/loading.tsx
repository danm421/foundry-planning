// Sub-tab loading boundary for the cashflow section. Sits below cashflow/layout.tsx
// (the subtab strip / sidebar), so navigating between cashflow sub-tabs keeps the
// subtabs in place and paints this skeleton in the content area instantly.
export { default } from "@/components/skeleton/section-loading";
