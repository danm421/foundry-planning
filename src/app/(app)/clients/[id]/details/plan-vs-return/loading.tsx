// Sub-tab loading boundary for the plan-vs-return section. Mirrors the
// tax-analysis/loading.tsx pattern — paints instantly in the content area while
// the server page resolves client access.
export { default } from "@/components/skeleton/section-loading";
