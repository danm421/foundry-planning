// Sub-tab loading boundary for the details section. Sits below details/layout.tsx
// (the subtab strip / sidebar), so navigating between details sub-tabs keeps the
// subtabs in place and paints this skeleton in the content area instantly.
export { default } from "@/components/skeleton/section-loading";
