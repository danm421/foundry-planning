// Loading boundary for the data-collection section (/data-collection and, via
// inheritance, /[id] and /email-settings). All are async, no-Suspense pages.
export { default } from "@/components/skeleton/list-loading";
