import type { ReactElement } from "react";
import { SharingContent } from "./sharing-content";

export const metadata = { title: "Sharing — Settings" };

export default function SharingSettingsPage(): ReactElement {
  return <SharingContent />;
}
