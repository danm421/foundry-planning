// Always one sheet: a hero number, three supporting figures, one match line and
// at most two sidebar tidbits never exceed a page.
export function estimateEarlyYearsStandingPageCount(): number {
  return 1;
}
