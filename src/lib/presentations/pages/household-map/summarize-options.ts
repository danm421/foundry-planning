// One line per page in the launcher's selected-pages list. No options to
// summarise, so each says what its page shows.

export function summarizeMapNetWorthOptions(): string {
  return "Who owns what";
}

export function summarizeMapCashFlowOptions(): string {
  return "Money in, saved, and out";
}

export function summarizeMapGoalsOptions(): string {
  return "Goals & milestones on a timeline";
}
