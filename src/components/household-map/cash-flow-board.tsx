import type { ReactElement } from "react";
import type { HouseholdMapProps } from "@/lib/household-map/types";

/** Stub — filled in by a later task. Typed to the full board prop object so
 *  `HouseholdMapView` can spread props into it today. */
const CashFlowBoard: (props: HouseholdMapProps) => ReactElement = () => (
  <div className="text-ink-3">Coming in the next task.</div>
);

export default CashFlowBoard;
