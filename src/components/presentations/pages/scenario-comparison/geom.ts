// Column geometry, exported so the layout guard derives its boxes from the SAME
// numbers the page lays out with. A guard that restates them measures a page the
// product never prints.
export const CONTENT_W = 526;   // PAGE_WIDTH_PORTRAIT 612 minus 2 x PAGE_PAD_X 43
export const LABEL_COL_W = 142;
export const VALUE_COL_W = 96;
export const MAX_COLUMNS = 4;
